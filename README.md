## Aplicación git-localconfig-repo

Se definen propiedades de los ambientes Dev, QA, default.

```
Cuando no se define una variable en los ambientes Dev, QA los valores de toman del archivo default.
```